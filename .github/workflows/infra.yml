name: Actions Runner Controller Demo
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: github-runner-set-infra
    steps:
    - name: Infra
      run: ls -lR